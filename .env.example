# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (Optional)
# If left blank or commented out, the application falls back safely to in-memory storage (MemStorage).
# DATABASE_URL=postgresql://username:password@localhost:5432/poker_cashout_db
